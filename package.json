{
  "name": "meshy-loaded-model-ripper",
  "version": "0.4.2",
  "description": "Chrome/Edge MV3 helper that captures Meshy loader GLB output, previews it, and exports validated optimized GLB or OBJ.",
  "private": true,
  "scripts": {
    "bundle:worker": "node scripts/bundle-worker.mjs",
    "check": "node --check extension/background.js && node --check extension/content.js && node --check extension/page-hook.js && node --check extension/popup.js && node --check extension/export-boot.js && node --check extension/export.js && node --check src/optimize-worker-source.js && node --check extension/optimize-worker.js",
    "build": "npm run bundle:worker && powershell -ExecutionPolicy Bypass -File ./build.ps1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ppotepa/meshy-loaded-model-ripper.git"
  },
  "keywords": [
    "chrome-extension",
    "edge-extension",
    "threejs",
    "glb",
    "gltf",
    "3d-models",
    "mesh-optimization",
    "webgl",
    "meshy-ai"
  ],
  "license": "UNLICENSED",
  "type": "module",
  "bugs": {
    "url": "https://github.com/ppotepa/meshy-loaded-model-ripper/issues"
  },
  "homepage": "https://github.com/ppotepa/meshy-loaded-model-ripper#readme",
  "devDependencies": {
    "@gltf-transform/core": "^4.4.0",
    "@gltf-transform/extensions": "^4.4.0",
    "esbuild": "^0.28.1",
    "meshoptimizer": "^1.1.1",
    "three": "^0.181.2"
  }
}
